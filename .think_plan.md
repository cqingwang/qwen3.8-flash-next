# Qwen3.8-Flash-Next 部署改造计划

## 范围

- 对齐 `../deepseek-flash` 的外层部署形态：`deploy.sh` 仅负责入口，`program.py` 负责命令分发与双机同步。
- 运行时唯一使用当前仓库 `dspark/start.sh`、`dspark/stop.sh` 及其子模块文件。
- 本地工作区是源，安装时同步到 head/worker 的 `/opt/qwen3.8-flash-next`。
- 模型不随项目同步，固定使用两台远程机器上的 `/opt/models/RadixArk/Qwen3.8-Flash-Next-NVFP4`。

## 拓扑与边界

- head：`spark-a` / `chan@192.168.2.180`
- worker：`spark-b` / `chan@192.168.2.161`
- 管理 SSH 使用 worker 目标 `chan@192.168.2.161`；SGLang 的 RoCE 地址和网卡仍由 `dspark/.env` 或环境变量提供，避免凭管理网地址猜测 fabric 拓扑。
- `deploy.sh` 的远端编排只做源码同步和命令转发，不重复实现模型下载、镜像、NCCL、SGLang 参数或容器生命周期。

## 命令映射

- `install`：本地 rsync 到双机后，在 head 执行 `dspark/start.sh serve`。
- `fetch`：在 head 执行当前子模块的 `download`，模型 ID 固定由子模块脚本定义；不把模型放入项目目录。
- `restart` / `stop` / `status` / `smoke` / `doctor` / `logs`：直接转发到 head 的子模块脚本。
- `uninstall`：停止双机容器并禁用可能存在的 systemd 单元，不删除模型和源码。
- `gen-env`：由 `config.yaml` 的 `env` 节完整生成 `dspark/.env`；`.env.example` 仅作参考，不参与部署。

## 验证

- `bash -n deploy.sh dspark/start.sh dspark/stop.sh`
- `python3 -m py_compile program.py`
- `./deploy.sh --help` 与 `./deploy.sh doctor --help` 能输出帮助。
- 真实部署后的最终证据由子模块负责：容器状态、`/v1/models`、smoke 请求和双机日志。
